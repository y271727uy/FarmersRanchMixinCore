/*
package com.y271727uy.FRMC.coremod;

import net.minecraftforge.fml.loading.FMLLoader;
import org.objectweb.asm.ClassReader;
import org.objectweb.asm.ClassWriter;
import org.objectweb.asm.tree.ClassNode;

import java.util.function.BiFunction;


 //核心字节码转换器
 //此类通过 FRMCCoordinator 注册到 ModLauncher 的 TransformationService 中。

public class FRMCClassTransformer implements BiFunction<String, ClassNode, ClassNode> {

    @Override
    public ClassNode apply(String className, ClassNode classNode) {
        // 在这里编写你的 ASM 树形 API 逻辑
        // 例如：if (className.equals("net/minecraft/world/item/crafting/RecipeManager")) { ... }
        return classNode;
    }
}

*/